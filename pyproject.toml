[project]
name = "ableton-offline-render"
version = "0.1.0"
description = "Offline batch renderer for Ableton Live sets"
requires-python = ">=3.11"
dependencies = [
    "lxml>=5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
